import TableSkeleton from '@/shared/ui/table-skeleton';

const ProductTableSkeleton = () => {
  return <TableSkeleton columns={5} />;
};

ProductTableSkeleton.displayName = 'ProductTable.Skeleton';

export default ProductTableSkeleton;
